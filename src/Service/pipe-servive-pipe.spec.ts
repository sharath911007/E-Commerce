import { PipeServivePipe } from './pipe-servive-pipe';

describe('PipeServivePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeServivePipe();
    expect(pipe).toBeTruthy();
  });
});
